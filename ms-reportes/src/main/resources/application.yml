server:
  port: ${PORT:8089}

spring:
  application:
    name: ms-reportes
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:h2:mem:reportesdb}
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.H2Dialect
  h2:
    console:
      enabled: true
      path: /h2-console

springdoc:
  swagger-ui:
    path: /swagger
  api-docs:
    path: /api-docs
